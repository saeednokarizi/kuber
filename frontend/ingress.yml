apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: frontend-ingress
spec:
  rules:
  - http:
    - path: /test/path
      pathType: prefix
      backend:
        service:
          name: test
          port:
            number: 8080